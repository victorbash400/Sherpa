{
  "name": "sherpa",
  "private": true,
  "type": "module",
  "scripts": {
    "build:native": "mkdir -p dist-native && xcrun swiftc -parse-as-library native/WindowCapture.swift -O -o dist-native/window-capture -framework ScreenCaptureKit -framework CoreImage -framework ImageIO -framework AppKit",
    "dev": "pnpm build:native && tsc -p electron/tsconfig.json && concurrently -k \"vite\" \"wait-on tcp:5173 && electron .\"",
    "build": "pnpm build:native && tsc && vite build && tsc -p electron/tsconfig.json",
    "typecheck": "tsc --noEmit && tsc -p electron/tsconfig.json --noEmit"
  },
  "main": "dist-electron/main.js",
  "dependencies": {
    "@playwright/mcp": "^0.0.79",
    "@steipete/peekaboo": "^4.0.0",
    "@vitejs/plugin-react": "latest",
    "concurrently": "latest",
    "electron": "latest",
    "lucide-react": "^1.31.0",
    "ogl": "^1.0.11",
    "react": "latest",
    "react-dom": "latest",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "typescript": "latest",
    "vite": "latest",
    "wait-on": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
